
/**
 * Returns a string of random hex characters of size length
 * @param {number} size requested size of string
 * @return {string} randomly generated hex string
 */
const {random_hex} = require('./fragments/random-hex')

module.exports = {
    random_hex,
}
